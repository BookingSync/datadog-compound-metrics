# frozen_string_literal: true

RSpec.describe Datadog::Compound::Metrics do
  it "has a version number" do
    expect(Datadog::Compound::Metrics::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
